spring.application.name=codifica
spring.datasource.url=jdbc:mysql://localhost/critix
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Altera a estrutura da tabela caso a entidade  tenha mudança
spring.jpa.hibernate.ddl-auto=update

api.security.token.secret = ${JWT_SECRET:12345678}

# Configuração para enviar email usando o meu email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thiagosena316@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
